{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist/my-angular-weather-app/browser",
  "routes": [
    {"src": "/(.*)", "dest": "/index.html"}
  ]
}
